<?php

$servername = "canteen_system";
$serverhost = 'localhost';
$serverpassword = '';
$serveruser = "root";
$conn = mysqli_connect($serverhost, $serveruser, $serverpassword, $servername);
if ($conn) {
    // echo 'Connected';
}
